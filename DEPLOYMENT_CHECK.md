# Инструкция по проверке деплоя

## Как проверить, что деплой прошел успешно:

### 1. Проверка в GitHub Actions

1. Откройте репозиторий на GitHub
2. Перейдите в раздел **Actions**
3. Найдите последний запуск workflow "Deploy to VPS"
4. Проверьте логи выполнения:
   - Убедитесь, что все шаги завершились успешно (зеленые галочки)
   - Проверьте вывод шага "Create .env file and deploy"
   - Найдите в логах:
     - `📋 New commit hash: ...` - хеш коммита, который был развернут
     - `Container commit: ...` - хеш коммита в контейнере
     - Убедитесь, что они совпадают

### 2. Проверка на сервере (через SSH)

Подключитесь к серверу:
```bash
ssh user@your-server-ip
cd /opt/appointment-system
```

Проверьте текущий коммит:
```bash
git log --oneline -1
git rev-parse HEAD
```

Проверьте статус контейнеров:
```bash
docker-compose ps
# или
docker compose ps
```

Проверьте логи контейнера web:
```bash
docker-compose logs --tail=50 web
# или
docker compose logs --tail=50 web
```

Проверьте код внутри контейнера:
```bash
docker-compose exec web git rev-parse HEAD
# или
docker compose exec web git rev-parse HEAD
```

Проверьте файлы внутри контейнера:
```bash
docker-compose exec web ls -la /app/appoinment_sistem/
# или
docker compose exec web ls -la /app/appoinment_sistem/
```

### 3. Проверка через веб-интерфейс

1. Откройте сайт в браузере
2. Обновите страницу с очисткой кэша (Ctrl+F5 или Ctrl+Shift+R)
3. Проверьте изменения в интерфейсе

### 4. Возможные проблемы и решения

#### Проблема: Код не обновился на сервере

**Проверка:**
```bash
cd /opt/appointment-system
git status
git log --oneline -5
```

**Решение:**
```bash
git fetch origin
git reset --hard origin/main
```

#### Проблема: Контейнер не перезапустился

**Проверка:**
```bash
docker-compose ps
docker-compose logs web
```

**Решение:**
```bash
docker-compose restart web
# или принудительно
docker-compose stop web
docker-compose up -d web
```

#### Проблема: Gunicorn использует старый код

**Решение:**
```bash
# Перезапустите контейнер полностью
docker-compose restart web

# Или пересоздайте контейнер
docker-compose up -d --force-recreate web
```

#### Проблема: Статические файлы не обновились

**Решение:**
```bash
docker-compose exec web python manage.py collectstatic --noinput
docker-compose restart web
```

### 5. Ручной деплой (если нужно)

Если автоматический деплой не сработал, выполните вручную:

```bash
cd /opt/appointment-system

# Обновить код
git fetch origin
git checkout main
git reset --hard origin/main

# Остановить контейнеры
docker-compose down

# Пересобрать образы
docker-compose build --no-cache web

# Запустить контейнеры
docker-compose up -d --force-recreate web

# Подождать запуска
sleep 15

# Применить миграции
docker-compose exec web python manage.py migrate

# Собрать статику
docker-compose exec web python manage.py collectstatic --noinput

# Перезапустить web контейнер
docker-compose restart web

# Проверить статус
docker-compose ps
docker-compose logs --tail=50 web
```

### 6. Проверка версии через API (если добавлен endpoint)

Если в проекте есть endpoint для проверки версии, можно проверить:
```bash
curl http://your-server-ip/api/version/
```

---

**Важно:** Все команды проверяйте дважды - возможно на сервере используется `docker compose` (v2) вместо `docker-compose` (v1).

# Инструкция по настройке Telegram бота

## 📋 Содержание
1. [Настройка бота](#настройка-бота)
2. [Установка зависимостей](#установка-зависимостей)
3. [Настройка переменных окружения](#настройка-переменных-окружения)
4. [Запуск бота локально](#запуск-бота-локально)
5. [Деплой на сервер](#деплой-на-сервер)
6. [Мини-приложение для записи](#мини-приложение-для-записи)

## 🤖 Настройка бота

1. Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
2. Получите токен бота
3. Добавьте токен в `settings.py`:
   ```python
   TELEGRAM_BOT_TOKEN = "ваш_токен_здесь"
   ```

## 📦 Установка зависимостей

```bash
pip install -r requirements.txt
```

## 🔧 Настройка переменных окружения

В файле `appoiment_system/settings.py` установите:

```python
# Токен бота (уже добавлен)
TELEGRAM_BOT_TOKEN = "8590756308:AAE6xuAbwAk7delCmWM37hTnr5cQR5ZsDhw"

# URL сайта для мини-приложения
SITE_URL = "https://yourdomain.com"  # Для production
# SITE_URL = "http://127.0.0.1:8000"  # Для разработки

# Использовать webhook вместо long polling (опционально)
TELEGRAM_USE_WEBHOOK = False  # True для production с HTTPS
TELEGRAM_WEBHOOK_URL = "https://yourdomain.com/api/telegram/webhook/"
```

## 🚀 Запуск бота локально

### Вариант 1: Через management команду (рекомендуется)

```bash
python manage.py run_bot
```

### Вариант 2: Через отдельный процесс

```bash
# В отдельном терминале
python manage.py run_bot &
```

## 🌐 Деплой на сервер

### Автоматический деплой

Используйте скрипт `deploy_bot.sh`:

```bash
chmod +x deploy_bot.sh
./deploy_bot.sh
```

Скрипт автоматически:
- Установит зависимости
- Применит миграции
- Создаст systemd service
- Настроит автозапуск
- Запустит бота

### Ручная настройка

1. **Создайте systemd service файл** `/etc/systemd/system/telegram-bot.service`:

```ini
[Unit]
Description=Telegram Bot for Appointment System
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/project
Environment="PATH=/path/to/project/venv/bin"
ExecStart=/path/to/project/venv/bin/python /path/to/project/manage.py run_bot
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

2. **Активируйте и запустите сервис**:

```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot.service
sudo systemctl start telegram-bot.service
```

3. **Проверьте статус**:

```bash
sudo systemctl status telegram-bot.service
```

4. **Просмотр логов**:

```bash
sudo journalctl -u telegram-bot -f
```

## 📱 Мини-приложение для записи

Бот поддерживает мини-приложение (Web App) для записи на консультацию прямо из Telegram.

### Как это работает:

1. Пользователь нажимает кнопку "📱 Записаться на консультацию" в боте
2. Открывается веб-приложение внутри Telegram
3. Пользователь выбирает услугу и временной слот
4. Заполняет форму и отправляет запись
5. Получает уведомление о подтверждении

### Настройка мини-приложения:

1. Убедитесь, что `SITE_URL` в `settings.py` указывает на ваш домен с HTTPS
2. Мини-приложение доступно по адресу: `https://yourdomain.com/telegram/appointment/`
3. Telegram автоматически проверяет SSL сертификат

### Тестирование локально:

Для тестирования мини-приложения локально используйте ngrok:

```bash
# Установите ngrok
# Запустите Django сервер
python manage.py runserver

# В другом терминале запустите ngrok
ngrok http 8000

# Используйте HTTPS URL от ngrok в SITE_URL
```

## 🔐 Безопасность

1. **Токен бота**: Храните в секретах GitHub или переменных окружения
2. **Webhook**: Используйте HTTPS для webhook URL
3. **Валидация**: Все данные из мини-приложения валидируются на сервере

## 📝 API Endpoints

- `GET /telegram/appointment/` - Мини-приложение для записи
- `POST /telegram/appointment/create/` - Создание записи
- `GET /telegram/api/slots/` - Получение доступных слотов
- `POST /api/telegram/webhook/` - Webhook для получения обновлений

## 🛠 Управление ботом

### Команды бота:

- `/start` - Начать работу с ботом
- `/register` - Регистрация в системе
- `/appointments` - Мои записи
- `/help` - Справка

### Кнопки:

- **📱 Записаться на консультацию** - Открывает мини-приложение
- **📋 Мои записи** - Показывает список записей
- **❌ Отменить** - Отменяет запись

## 🐛 Решение проблем

### Бот не отвечает:

1. Проверьте, что бот запущен: `sudo systemctl status telegram-bot`
2. Проверьте логи: `sudo journalctl -u telegram-bot -f`
3. Убедитесь, что токен правильный
4. Проверьте интернет-соединение

### Мини-приложение не открывается:

1. Убедитесь, что используется HTTPS
2. Проверьте, что `SITE_URL` правильный
3. Проверьте SSL сертификат
4. Убедитесь, что домен доступен извне

### Ошибки при создании записи:

1. Проверьте логи Django
2. Убедитесь, что все поля формы заполнены
3. Проверьте, что слот доступен
4. Проверьте миграции базы данных

## 📞 Поддержка

При возникновении проблем проверьте:
- Логи бота: `sudo journalctl -u telegram-bot -f`
- Логи Django: настройте логирование в `settings.py`
- Статус сервиса: `sudo systemctl status telegram-bot`


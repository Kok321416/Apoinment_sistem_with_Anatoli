---
description: Public booking funnel — specialist pages and client book flow
globs: app/templates/public/**/*.html,app/static/css/booking.css,app/static/css/public-specialist.css,app/static/js/public-*.js,app/routers/public_specialist.py,app/services/public_client.py,app/services/slots.py
alwaysApply: false
---

# Public Booking agent

## Scope

- `/s/{slug}/…` public pages, welcome/gate, calendar book
- `booking.css`, `public-*.js`, `public_specialist` router/services, slots for clients

## Coordination

- Auth gate UI: align with Auth & OAuth / Visual login look.
- Availability math: Backend/slots — do not fork slot algorithms.

## DoD

- Guest → login → return to booking works.
- Mobile booking steps readable; labels match product copy.
- No desktop admin leakage into public templates.

# 🚀 Пошаговая инструкция: Запуск HTTPS

## Шаг 1: Коммит и push изменений (на вашем компьютере)

```bash
git commit -m "Add HTTPS support with self-signed certificate"
git push origin develop
```

GitHub Actions автоматически обновит код на сервере.

## Шаг 2: Подключитесь к серверу

```bash
ssh root@YOUR_VPS_IP
```

## Шаг 3: Обновите .env файл на сервере

```bash
cd /opt/appointment-system
nano .env
```

**Добавьте или обновите эти строки:**

```env
USE_HTTPS=True
ALLOWED_HOSTS=ваш_vps_ip
```

**Пример:**
```env
USE_HTTPS=True
ALLOWED_HOSTS=185.155.101.22
```

Сохраните файл: `Ctrl+O`, `Enter`, `Ctrl+X`

## Шаг 4: Создайте директории для сертификатов

```bash
cd /opt/appointment-system
mkdir -p certbot/conf certbot/www
chmod -R 755 certbot
```

## Шаг 5: Остановите старые контейнеры и запустите новые

```bash
cd /opt/appointment-system

# Остановите старые контейнеры
docker-compose down

# Если код еще не обновился, обновите вручную
git pull origin develop

# Запустите контейнеры
docker-compose up -d
```

**⚠️ ВАЖНО:** При первом запуске nginx автоматически создаст самоподписанный сертификат.

## Шаг 6: Проверьте логи и статус

```bash
# Проверьте статус контейнеров
docker-compose ps

# Должны быть запущены: db, web, nginx, certbot, pgadmin

# Проверьте логи nginx (должно показать создание сертификата)
docker-compose logs nginx | tail -30

# Ищите строку: "Self-signed certificate created"
```

## Шаг 7: Проверьте работу HTTPS

```bash
# Проверьте, что порты открыты
ss -tlnp | grep -E ':(80|443)'

# Должны быть:
# :80 (HTTP - редирект на HTTPS)
# :443 (HTTPS)

# Проверьте, что сертификат создан
docker-compose exec nginx ls -la /etc/letsencrypt/live/selfsigned/

# Должны быть файлы: fullchain.pem и privkey.pem
```

## Шаг 8: Откройте в браузере

```
https://YOUR_VPS_IP
```

**Например:** `https://185.155.101.22`

## ⚠️ Важно: Предупреждение браузера

Браузер покажет предупреждение о самоподписанном сертификате. **Это нормально!**

**Как принять сертификат:**

### Chrome/Edge:
1. Нажмите "Дополнительно" / "Advanced"
2. Нажмите "Перейти на сайт" / "Proceed to site"

### Firefox:
1. Нажмите "Дополнительно" / "Advanced"
2. Нажмите "Принять риск и продолжить" / "Accept the Risk and Continue"

После этого сайт откроется и будет работать через HTTPS.

## ✅ Проверка успешной настройки

1. ✅ HTTP редиректит на HTTPS: `http://YOUR_VPS_IP` → `https://YOUR_VPS_IP`
2. ✅ В браузере показывается 🔒 (замочек) в адресной строке
3. ✅ URL начинается с `https://`
4. ✅ Все контейнеры запущены: `docker-compose ps`

## 🐛 Если что-то пошло не так

### Ошибка: "SSL certificate not found"
```bash
# Удалите директорию и перезапустите
rm -rf certbot/conf/live/selfsigned
docker-compose restart nginx
```

### Ошибка: "Port 443 already in use"
```bash
# Проверьте, что запущен только nginx из docker-compose
docker-compose ps
# Если другой nginx запущен, остановите его:
systemctl stop nginx
docker-compose restart nginx
```

### Nginx не запускается
```bash
# Проверьте логи
docker-compose logs nginx

# Проверьте конфигурацию
docker-compose exec nginx nginx -t
```

### HTTP не редиректит на HTTPS
```bash
# Проверьте, что в .env установлено USE_HTTPS=True
cat .env | grep USE_HTTPS

# Перезапустите контейнеры
docker-compose restart web nginx
```

## 🎉 Готово!

После выполнения всех шагов ваше приложение будет работать через HTTPS!

Все запросы на HTTP будут автоматически перенаправляться на HTTPS.

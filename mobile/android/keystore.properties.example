# Copy to keystore.properties (same folder) and fill passwords.
# keystore.properties is gitignored - never commit it.

storeFile=release.jks
storePassword=PASTE_KEYSTORE_PASSWORD
keyAlias=allyourclients
keyPassword=PASTE_KEY_PASSWORD

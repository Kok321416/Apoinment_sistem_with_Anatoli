# 🔐 Быстрая инструкция: Переменные для секретов

## ✅ Имя переменной для токена Telegram бота:

### **`TELEGRAM_BOT_TOKEN`**

Это стандартное имя переменной, которое нужно использовать во всех системах.

## 📝 Где добавить:

### GitHub Secrets:
1. Settings → Secrets and variables → Actions
2. New repository secret
3. **Name:** `TELEGRAM_BOT_TOKEN`
4. **Value:** `8590756308:AAE6xuAbwAk7delCmWM37hTnr5cQR5ZsDhw`

### GitLab CI/CD Variables:
1. Settings → CI/CD → Variables
2. Add variable
3. **Key:** `TELEGRAM_BOT_TOKEN`
4. **Value:** `8590756308:AAE6xuAbwAk7delCmWM37hTnr5cQR5ZsDhw`
5. ✅ Protected (рекомендуется)
6. ✅ Masked (рекомендуется)

### На сервере (через systemd):
```bash
sudo systemctl set-environment TELEGRAM_BOT_TOKEN="8590756308:AAE6xuAbwAk7delCmWM37hTnr5cQR5ZsDhw"
sudo systemctl daemon-reload
sudo systemctl restart telegram-bot
```

## 🔑 Все необходимые переменные:

| Переменная | Описание | Обязательная |
|------------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | ✅ Да |
| `SITE_URL` | URL сайта (для мини-приложения) | ⚠️ Опционально (по умолчанию http://127.0.0.1:8000) |
| `DEPLOY_HOST` | IP или домен сервера | ✅ Да (для CI/CD) |
| `DEPLOY_USER` | Пользователь SSH | ✅ Да (для CI/CD) |
| `DEPLOY_SSH_KEY` | Приватный SSH ключ | ✅ Да (для CI/CD) |
| `DEPLOY_PATH` | Путь к проекту на сервере | ✅ Да (для CI/CD) |

## ⚡ Быстрая настройка для деплоя:

### GitHub:
```
TELEGRAM_BOT_TOKEN = 8590756308:AAE6xuAbwAk7delCmWM37hTnr5cQR5ZsDhw
```

### GitLab:
```
TELEGRAM_BOT_TOKEN = 8590756308:AAE6xuAbwAk7delCmWM37hTnr5cQR5ZsDhw
```

**Примечание:** `SITE_URL` можно не указывать, будет использоваться значение по умолчанию. Укажите только если нужен другой URL для мини-приложения.

## ✅ Проверка:

После добавления секретов код автоматически будет читать токен из переменной окружения `TELEGRAM_BOT_TOKEN`.

**Важно:** Токен в `settings.py` используется только как fallback для локальной разработки. В production всегда используется переменная окружения.


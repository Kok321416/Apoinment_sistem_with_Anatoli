# План разработки: Система онлайн-записей на консультации

## 🎯 Цель проекта
Создать систему для онлайн-записей на консультации, похожую на Fastime, с нуля.

---

## 📚 ФАЗА 0: Подготовка и изучение основ (1-2 дня)

### Шаг 0.1: Настройка окружения
- [ ] Установить Python 3.10+ (если еще не установлен)
- [ ] Создать виртуальное окружение: `python -m venv venv`
- [ ] Активировать виртуальное окружение
- [ ] Установить Django: `pip install django`
- [ ] Создать файл `requirements.txt` с зависимостями

### Шаг 0.2: Изучение основ Django (2-3 часа самостоятельного изучения)
**Что нужно изучить:**
- Что такое Django и зачем он нужен
- Структура проекта Django (проект, приложение, модели, views, urls, templates)
- Базовые команды: `django-admin startproject`, `python manage.py runserver`
- Что такое MVT (Model-View-Template) архитектура

**Где искать информацию:**
- Официальная документация Django: https://docs.djangoproject.com/
- Django Tutorial: https://docs.djangoproject.com/en/stable/intro/tutorial01/
- YouTube: "Django для начинающих" (на русском)

**Задача:** После изучения объяснить себе словами, что такое модель, view и template.

---

## 📚 ФАЗА 1: Изучение базового HTML (1 день)

### Шаг 1.1: Основы HTML (2-3 часа)
**Что нужно изучить:**
- Структура HTML документа (`<!DOCTYPE html>`, `<html>`, `<head>`, `<body>`)
- Основные теги: `<h1>-<h6>`, `<p>`, `<div>`, `<span>`, `<a>`, `<img>`
- Списки: `<ul>`, `<ol>`, `<li>`
- Формы: `<form>`, `<input>`, `<button>`, `<select>`, `<textarea>`
- Семантические теги: `<header>`, `<nav>`, `<main>`, `<section>`, `<footer>`

**Где искать информацию:**
- MDN Web Docs: https://developer.mozilla.org/ru/docs/Web/HTML
- HTML Academy (базовый курс)
- W3Schools: https://www.w3schools.com/html/

**Практическое задание:** Создать простую HTML страницу с заголовком, параграфом, списком и формой (без стилей, только структура).

### Шаг 1.2: Основы CSS (2-3 часа)
**Что нужно изучить:**
- Что такое CSS и как его подключать (внутри `<style>`, отдельный файл, inline)
- Селекторы: по тегу, по классу (`.`), по ID (`#`)
- Основные свойства: `color`, `background-color`, `font-size`, `margin`, `padding`, `width`, `height`
- Блочная модель (box model): margin, border, padding, content
- Flexbox основы: `display: flex`, `flex-direction`, `justify-content`, `align-items`

**Где искать информацию:**
- MDN Web Docs CSS: https://developer.mozilla.org/ru/docs/Web/CSS
- CSS-Tricks: https://css-tricks.com/
- Flexbox Froggy (игра для изучения Flexbox): https://flexboxfroggy.com/

**Практическое задание:** Стилизовать страницу из шага 1.1, используя классы и базовые свойства CSS.

---

## 🏗️ ФАЗА 2: Бэкенд - Создание проекта и базовой структуры

### Шаг 2.1: Создание Django проекта
**Задача:** Создать проект Django и базовое приложение

**Что нужно сделать:**
1. Создать проект: `django-admin startproject appointment_system .`
2. Создать приложение: `python manage.py startapp bookings`
3. Добавить приложение в `settings.py` в `INSTALLED_APPS`
4. Запустить сервер: `python manage.py runserver`
5. Убедиться, что видна страница "It worked!"

**Что изучить:**
- Структуру папок Django проекта
- Что такое `settings.py`, `urls.py`, `wsgi.py`
- Что такое миграции и зачем они нужны

**Проверка:** После выполнения - показать структуру проекта и объяснить назначение основных файлов.

---

### Шаг 2.2: Создание первой модели (Consultant - Консультант)
**Задача:** Создать модель для хранения информации о консультантах

**Что нужно сделать:**
1. В `models.py` создать модель `Consultant` с полями:
   - `name` (CharField) - имя
   - `email` (EmailField) - email
   - `specialization` (CharField) - специализация
   - `created_at` (DateTimeField) - дата создания
   
2. Создать миграцию: `python manage.py makemigrations`
3. Применить миграцию: `python manage.py migrate`
4. Зарегистрировать модель в `admin.py` для просмотра в админке

**Что изучить:**
- Типы полей Django: CharField, EmailField, DateTimeField, IntegerField, ForeignKey
- Что такое миграции и как они работают
- Django Admin панель

**Где искать информацию:**
- Django Models: https://docs.djangoproject.com/en/stable/topics/db/models/
- Django Admin: https://docs.djangoproject.com/en/stable/ref/contrib/admin/

**Проверка:** Создать несколько консультантов через админ-панель Django.

---

### Шаг 2.3: Создание модели Appointment (Запись)
**Задача:** Создать модель для записей на консультации

**Что нужно сделать:**
1. Создать модель `Appointment` с полями:
   - `consultant` (ForeignKey к Consultant) - консультант
   - `client_name` (CharField) - имя клиента
   - `client_email` (EmailField) - email клиента
   - `appointment_date` (DateTimeField) - дата и время записи
   - `status` (CharField с choices) - статус (pending, confirmed, cancelled)
   - `created_at` (DateTimeField) - дата создания записи

2. Создать и применить миграции
3. Зарегистрировать в админке

**Что изучить:**
- ForeignKey и связи между моделями
- Choices в Django (для статусов)

**Проверка:** Создать несколько записей через админку, убедиться что связи работают.

---

## 🌐 ФАЗА 3: Создание первых страниц (HTML без стилей)

### Шаг 3.1: Настройка шаблонов Django
**Задача:** Настроить систему шаблонов и создать базовый шаблон

**Что нужно сделать:**
1. Создать папку `templates` в корне проекта
2. Создать папку `templates/bookings` для шаблонов приложения
3. В `settings.py` настроить `TEMPLATES` (обычно уже настроено)
4. Создать базовый шаблон `base.html` с минимальной структурой:
   ```html
   <!DOCTYPE html>
   <html>
   <head>
       <title>{% block title %}Записи на консультации{% endblock %}</title>
   </head>
   <body>
       {% block content %}
       {% endblock %}
   </body>
   </html>
   ```

**Что изучить:**
- Django Template Language (DTL)
- Блочная система шаблонов (`{% block %}`, `{% extends %}`)
- Как работает наследование шаблонов

**Где искать информацию:**
- Django Templates: https://docs.djangoproject.com/en/stable/topics/templates/

**Проверка:** Проверить, что базовый шаблон создан и настроен.

---

### Шаг 3.2: Создание страницы списка консультантов
**Задача:** Создать простую HTML страницу, которая показывает список всех консультантов

**Что нужно сделать:**
1. В `views.py` создать функцию `consultant_list`:
   - Получить всех консультантов из базы: `Consultant.objects.all()`
   - Передать их в шаблон через `context`
   
2. В `urls.py` (приложения) добавить маршрут для этой страницы
3. В `urls.py` (проекта) подключить URL приложения
4. Создать шаблон `consultant_list.html`, который:
   - Наследуется от `base.html`
   - Использует цикл `{% for %}` для вывода списка консультантов
   - Показывает имя, email и специализацию каждого консультанта

**Что изучить:**
- Django Views (функциональные представления)
- Django URLs (маршрутизация)
- Template tags: `{% for %}`, `{{ variable }}`

**Проверка:** Открыть страницу в браузере, убедиться что список консультантов отображается (даже без стилей).

---

### Шаг 3.3: Создание страницы деталей консультанта
**Задача:** Создать страницу, которая показывает информацию об одном консультанте

**Что нужно сделать:**
1. Создать view `consultant_detail(request, consultant_id)`:
   - Получить консультанта по ID: `Consultant.objects.get(id=consultant_id)`
   - Передать в шаблон
   
2. Добавить URL маршрут с параметром `consultant_id`
3. Создать шаблон `consultant_detail.html`:
   - Показать всю информацию о консультанте
   - Добавить ссылку назад к списку

**Что изучить:**
- Как передавать параметры через URL
- Обработка ошибок (что если консультант не найден?)

**Проверка:** Открыть страницу конкретного консультанта, проверить что информация отображается.

---

## 🎨 ФАЗА 4: Добавление базовых стилей (CSS)

### Шаг 4.1: Подключение CSS и создание базовых стилей
**Задача:** Добавить CSS файл и стилизовать базовый шаблон

**Что нужно сделать:**
1. Создать папку `static` в корне проекта
2. Создать папку `static/css`
3. Создать файл `static/css/style.css`
4. В `settings.py` проверить настройку `STATIC_URL` и `STATICFILES_DIRS`
5. В `base.html` подключить CSS файл: `{% load static %}` и `<link rel="stylesheet" href="{% static 'css/style.css' %}">`
6. Добавить базовые стили:
   - Сброс стилей (reset или normalize)
   - Базовые стили для body, заголовков, параграфов
   - Простой layout (header, main, footer)

**Что изучить:**
- Как Django работает со статическими файлами
- `{% load static %}` и `{% static %}` теги
- Базовые принципы верстки

**Проверка:** Убедиться что CSS файл загружается и применяется.

---

### Шаг 4.2: Стилизация страницы списка консультантов
**Задача:** Сделать список консультантов красивым и читаемым

**Что нужно сделать:**
1. В шаблоне `consultant_list.html` добавить классы к элементам
2. В CSS создать стили для:
   - Контейнера списка
   - Карточки консультанта (card)
   - Использовать Flexbox для расположения карточек
   - Добавить отступы, цвета, границы

**Что изучить:**
- Flexbox для расположения элементов
- Создание карточек (cards) в CSS
- Цветовая схема и типографика

**Проверка:** Список должен выглядеть аккуратно, карточки должны быть расположены в ряд.

---

### Шаг 4.3: Стилизация страницы деталей консультанта
**Задача:** Стилизовать страницу с информацией о консультанте

**Что нужно сделать:**
1. Добавить классы к элементам в шаблоне
2. Создать стили для:
   - Заголовка страницы
   - Информационного блока
   - Кнопки "Назад"
   - Использовать контейнер для ограничения ширины

**Проверка:** Страница должна выглядеть аккуратно и читабельно.

---

## 📝 ФАЗА 5: Создание формы записи (базовая версия)

### Шаг 5.1: Изучение Django Forms
**Задача:** Изучить основы работы с формами в Django

**Что нужно изучить:**
- Что такое Django Forms
- Как создать форму: `forms.py`
- Типы полей форм: `CharField`, `EmailField`, `DateTimeField`, `ChoiceField`
- Валидация форм

**Где искать информацию:**
- Django Forms: https://docs.djangoproject.com/en/stable/topics/forms/

---

### Шаг 5.2: Создание формы записи на консультацию
**Задача:** Создать форму для записи на консультацию

**Что нужно сделать:**
1. Создать файл `forms.py` в приложении
2. Создать форму `AppointmentForm` с полями:
   - `client_name` (CharField)
   - `client_email` (EmailField)
   - `appointment_date` (DateTimeField)
   - `consultant` (ChoiceField или ModelChoiceField)

3. Создать view `create_appointment`:
   - Если GET запрос - показать пустую форму
   - Если POST запрос - обработать форму, сохранить в базу, перенаправить

4. Создать шаблон `create_appointment.html`:
   - Показать форму через `{{ form.as_p }}` или вручную
   - Добавить кнопку отправки

5. Добавить URL маршрут

**Что изучить:**
- Обработка GET и POST запросов
- Редиректы в Django (`redirect()`)
- CSRF защита в формах Django

**Проверка:** Заполнить форму, отправить, проверить что запись сохранилась в базе (через админку).

---

### Шаг 5.3: Стилизация формы
**Задача:** Сделать форму красивой и удобной

**Что нужно сделать:**
1. Добавить классы к полям формы в шаблоне
2. Создать CSS стили для:
   - Поля ввода (`input`, `select`, `textarea`)
   - Кнопки отправки
   - Сообщений об ошибках
   - Группировки полей (label + input)

**Что изучить:**
- Стилизация форм в CSS
- Псевдо-классы: `:focus`, `:hover`
- Отображение ошибок валидации

**Проверка:** Форма должна выглядеть современно и быть удобной для заполнения.

---

## 📅 ФАЗА 6: Создание страницы со списком записей

### Шаг 6.1: Страница списка всех записей
**Задача:** Создать страницу, которая показывает все записи

**Что нужно сделать:**
1. Создать view `appointment_list`:
   - Получить все записи: `Appointment.objects.all()`
   - Отсортировать по дате
   - Передать в шаблон

2. Создать шаблон `appointment_list.html`:
   - Таблица или список записей
   - Показать: имя клиента, консультанта, дату, статус

3. Добавить URL и стили

**Проверка:** Отображаются все записи в понятном виде.

---

### Шаг 6.2: Фильтрация записей (опционально)
**Задача:** Добавить возможность фильтровать записи по статусу

**Что нужно сделать:**
1. В view добавить обработку GET параметра `status`
2. Фильтровать записи: `Appointment.objects.filter(status=status)`
3. В шаблоне добавить ссылки/кнопки для фильтрации

**Проверка:** Фильтрация работает корректно.

---

## 🔐 ФАЗА 7: Система аутентификации (базовая)

### Шаг 7.1: Изучение Django Authentication
**Задача:** Изучить встроенную систему аутентификации Django

**Что нужно изучить:**
- Django User модель
- `authenticate()`, `login()`, `logout()` функции
- Декораторы `@login_required`
- `UserCreationForm`

**Где искать информацию:**
- Django Authentication: https://docs.djangoproject.com/en/stable/topics/auth/

---

### Шаг 7.2: Страница регистрации
**Задача:** Создать страницу для регистрации новых пользователей

**Что нужно сделать:**
1. Создать view `register`:
   - Использовать `UserCreationForm`
   - Обработать POST запрос
   - После регистрации автоматически войти пользователя

2. Создать шаблон `register.html`
3. Добавить URL и стили

**Проверка:** Можно зарегистрировать нового пользователя.

---

### Шаг 7.3: Страницы входа и выхода
**Задача:** Создать страницы для входа и выхода

**Что нужно сделать:**
1. Создать view `login_view`:
   - Использовать `AuthenticationForm`
   - Обработать POST, авторизовать пользователя

2. Создать view `logout_view`:
   - Вызвать `logout()`
   - Перенаправить на главную

3. Создать шаблоны `login.html` и обновить `base.html` (добавить кнопки входа/выхода)
4. Добавить URL

**Проверка:** Можно войти и выйти из системы.

---

### Шаг 7.4: Защита страниц (только для авторизованных)
**Задача:** Ограничить доступ к созданию записей только авторизованным пользователям

**Что нужно сделать:**
1. Добавить декоратор `@login_required` к view создания записи
2. Настроить `LOGIN_URL` в `settings.py`
3. Обновить навигацию в `base.html` (показывать разные ссылки для авторизованных/неавторизованных)

**Что изучить:**
- Декораторы в Python
- `@login_required` декоратор
- `{% if user.is_authenticated %}` в шаблонах

**Проверка:** Неавторизованный пользователь не может создать запись, перенаправляется на страницу входа.

---

## 🎨 ФАЗА 8: Улучшение дизайна и UX

### Шаг 8.1: Навигация и header
**Задача:** Создать удобную навигацию по сайту

**Что нужно сделать:**
1. В `base.html` создать header с навигацией:
   - Логотип/название сайта
   - Ссылки: Главная, Консультанты, Мои записи, Войти/Выйти
   
2. Добавить CSS стили для навигации:
   - Горизонтальное меню (Flexbox)
   - Стили для активных ссылок
   - Адаптивность (базовая)

**Проверка:** Навигация работает на всех страницах.

---

### Шаг 8.2: Улучшение цветовой схемы и типографики
**Задача:** Сделать сайт более привлекательным визуально

**Что нужно сделать:**
1. Выбрать цветовую палитру (2-3 основных цвета)
2. Улучшить типографику:
   - Выбрать шрифты (Google Fonts)
   - Настроить размеры заголовков, текста
   - Межстрочные интервалы
   
3. Добавить тени, скругления, переходы

**Что изучить:**
- Google Fonts
- CSS transitions
- Цветовые схемы для веб-дизайна

**Проверка:** Сайт выглядит современно и профессионально.

---

### Шаг 8.3: Сообщения пользователю (flash messages)
**Задача:** Добавить сообщения об успешных операциях и ошибках

**Что нужно сделать:**
1. Использовать Django messages framework
2. В view после успешных операций добавить: `messages.success(request, 'Сообщение')`
3. В `base.html` отобразить сообщения:
   ```html
   {% if messages %}
       {% for message in messages %}
           <div class="alert alert-{{ message.tags }}">{{ message }}</div>
       {% endfor %}
   {% endif %}
   ```
4. Добавить CSS стили для сообщений

**Проверка:** После создания записи появляется сообщение об успехе.

---

## 📱 ФАЗА 9: Календарь и выбор времени (базовая версия)

### Шаг 9.1: Изучение работы с датами в Django
**Задача:** Изучить как работать с датами и временем

**Что нужно изучить:**
- Python `datetime` модуль
- Django `timezone` utilities
- Форматирование дат в шаблонах (`{% now %}` фильтр, `date` фильтр)

---

### Шаг 9.2: Простая страница выбора даты
**Задача:** Создать страницу, где можно выбрать дату для записи

**Что нужно сделать:**
1. Создать view, который принимает `consultant_id`
2. Создать шаблон с простым выбором даты:
   - Можно использовать `<input type="date">`
   - Или список доступных дат (ближайшие 7 дней)
   
3. После выбора даты переходить к выбору времени

**Проверка:** Можно выбрать дату для записи.

---

### Шаг 9.3: Выбор времени (базовая версия)
**Задача:** Показать доступные временные слоты

**Что нужно сделать:**
1. Создать view, который:
   - Принимает `consultant_id` и `date`
   - Генерирует список доступных времен (например, каждый час с 9:00 до 18:00)
   - Исключает уже занятые времена (проверить существующие записи)

2. Создать шаблон с кнопками времени
3. После выбора времени переходить к форме заполнения данных

**Что изучить:**
- Генерация временных слотов
- Проверка доступности слотов
- Работа с временем в Python

**Проверка:** Показываются только доступные временные слоты.

---

## 🔄 ФАЗА 10: Улучшения и доработки

### Шаг 10.1: Страница "Мои записи"
**Задача:** Создать страницу, где пользователь видит свои записи

**Что нужно сделать:**
1. Связать записи с пользователями (если еще не сделано):
   - Добавить поле `user` (ForeignKey to User) в модель Appointment
   - Обновить форму создания записи
   
2. Создать view `my_appointments`:
   - Фильтровать записи по текущему пользователю
   - `Appointment.objects.filter(user=request.user)`

3. Создать шаблон и стили

**Проверка:** Пользователь видит только свои записи.

---

### Шаг 10.2: Отмена записи
**Задача:** Добавить возможность отменить запись

**Что нужно сделать:**
1. Создать view `cancel_appointment`:
   - Проверить что запись принадлежит пользователю
   - Изменить статус на "cancelled" или удалить
   
2. Добавить кнопку "Отменить" в шаблоне "Мои записи"
3. Добавить подтверждение (можно через JavaScript или просто страница подтверждения)

**Проверка:** Можно отменить свою запись.

---

### Шаг 10.3: Улучшение формы (валидация даты)
**Задача:** Добавить проверку, что нельзя записаться на прошедшую дату

**Что нужно сделать:**
1. В форме добавить валидацию:
   - Создать метод `clean_appointment_date()` в форме
   - Проверить что дата в будущем
   - Вызвать `ValidationError` если дата в прошлом

2. Добавить проверку на пересечение с другими записями

**Проверка:** Нельзя создать запись на прошедшую дату.

---

## 🚀 ФАЗА 11: Финальные улучшения

### Шаг 11.1: Адаптивный дизайн (базовая версия)
**Задача:** Сделать сайт адаптивным для мобильных устройств

**Что нужно сделать:**
1. Добавить viewport meta тег в `base.html`
2. Использовать медиа-запросы в CSS:
   ```css
   @media (max-width: 768px) {
       /* стили для мобильных */
   }
   ```
3. Адаптировать навигацию, карточки, формы

**Что изучить:**
- Медиа-запросы в CSS
- Mobile-first подход
- Responsive design принципы

**Проверка:** Сайт удобно просматривать на мобильном устройстве.

---

### Шаг 11.2: Обработка ошибок
**Задача:** Добавить обработку ошибок (404, 500)

**Что нужно сделать:**
1. Создать шаблоны `404.html` и `500.html`
2. В `settings.py` настроить `DEBUG = False` для проверки (временно)
3. Добавить try-except блоки в views где нужно

**Проверка:** При ошибках показываются понятные страницы.

---

### Шаг 11.3: Финальная проверка и рефакторинг
**Задача:** Проверить весь код, улучшить структуру

**Что нужно сделать:**
1. Проверить все страницы на наличие багов
2. Улучшить читаемость кода
3. Добавить комментарии где нужно
4. Проверить безопасность (CSRF, XSS защита Django)
5. Оптимизировать запросы к базе данных (если нужно)

**Проверка:** Весь функционал работает корректно.

---

## 📝 Дополнительные улучшения (после базовой версии)

### Возможные улучшения:
- [ ] Поиск консультантов по специализации
- [ ] Email уведомления о записях
- [ ] Календарь с визуализацией (JavaScript библиотека)
- [ ] Профиль консультанта с фото и описанием
- [ ] Рейтинги и отзывы
- [ ] Панель администратора для управления записями
- [ ] Экспорт данных в CSV/Excel
- [ ] API для мобильного приложения (Django REST Framework)

---

## 📌 Важные замечания

1. **Работайте по одному шагу за раз** - не переходите к следующему, пока не завершили текущий
2. **Пишите код самостоятельно** - используйте план как руководство, но код пишите сами
3. **Ищите информацию** - для каждого шага есть указания где искать информацию
4. **Тестируйте после каждого шага** - убедитесь что все работает перед переходом дальше
5. **Не бойтесь ошибок** - они часть обучения, разбирайте их и исправляйте
6. **Задавайте вопросы** - если что-то непонятно, ищите ответы в документации или спрашивайте

---

## ✅ Чеклист готовности к следующему шагу

Перед переходом к следующему шагу убедитесь:
- [ ] Код написан и работает
- [ ] Страница/функция отображается в браузере
- [ ] Нет критических ошибок
- [ ] Понимаете что делает ваш код
- [ ] Можете объяснить другому человеку что вы сделали

---

**Удачи в разработке! 🚀**


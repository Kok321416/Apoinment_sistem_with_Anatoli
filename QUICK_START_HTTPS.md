# Быстрый старт: Настройка HTTPS

## Автоматическая настройка (рекомендуется)

HTTPS настроен автоматически! При первом запуске `docker-compose up` самоподписанный сертификат будет создан автоматически.

### Шаг 1: Обновите .env файл

На сервере откройте `.env` файл и убедитесь, что установлено:

```env
USE_HTTPS=True
ALLOWED_HOSTS=ваш_vps_ip
```

### Шаг 2: Запустите контейнеры

```bash
cd /opt/appointment-system
docker-compose down
docker-compose up -d
```

### Шаг 3: Проверьте логи

```bash
docker-compose logs nginx | grep -i cert
# Должно показать: "Самоподписанный сертификат создан"
```

### Шаг 4: Откройте в браузере

```
https://YOUR_VPS_IP
```

⚠️ **Важно:** Браузер покажет предупреждение о самоподписанном сертификате. Это нормально!

**Как принять сертификат:**
1. Нажмите "Дополнительно" / "Advanced"
2. Нажмите "Перейти на сайт" / "Proceed to site"
3. Сертификат будет принят для этого IP

## Ручная настройка (если нужно пересоздать сертификат)

Если хотите пересоздать сертификат вручную:

```bash
cd /opt/appointment-system

# Сделайте скрипты исполняемыми
chmod +x scripts/generate-selfsigned-cert.sh
chmod +x scripts/setup-https.sh

# Создайте сертификат
./scripts/generate-selfsigned-cert.sh

# Перезапустите nginx
docker-compose restart nginx
```

## Что происходит автоматически

1. При первом запуске `docker-compose up`, nginx проверяет наличие сертификата
2. Если сертификата нет, создается самоподписанный сертификат
3. Nginx запускается с HTTPS конфигурацией
4. HTTP (порт 80) автоматически редиректит на HTTPS (порт 443)

## Переход на Let's Encrypt (если появится домен)

Если у вас появится домен и вы захотите использовать Let's Encrypt:

1. Обновите `nginx/conf.d/app.conf` - измените пути к сертификату
2. Запустите скрипт получения сертификата:
   ```bash
   chmod +x nginx/init-letsencrypt.sh
   ./nginx/init-letsencrypt.sh your-domain.com your-email@example.com
   ```
3. Обновите `.env`:
   ```env
   ALLOWED_HOSTS=your-domain.com,www.your-domain.com
   ```
4. Перезапустите: `docker-compose restart nginx web`

## Проверка работы HTTPS

```bash
# Проверьте, что порты открыты
ss -tlnp | grep -E ':(80|443)'

# Проверьте логи nginx
docker-compose logs nginx

# Проверьте, что сертификат существует
docker-compose exec nginx ls -la /etc/letsencrypt/live/selfsigned/

# Тест подключения
curl -k https://YOUR_VPS_IP
# -k игнорирует ошибку самоподписанного сертификата
```

## Решение проблем

### Ошибка: "SSL certificate not found"

```bash
# Удалите старый сертификат и перезапустите
rm -rf certbot/conf/live/selfsigned
docker-compose restart nginx
```

### Ошибка: "Port 443 already in use"

```bash
# Проверьте, что не запущен другой nginx
systemctl status nginx
# Если запущен, остановите: systemctl stop nginx
```

### Браузер не принимает сертификат

- Используйте другой браузер для теста
- Очистите кеш браузера
- Попробуйте в режиме инкогнито
- Проверьте, что открываете именно `https://` (не `http://`)

### HTTP не редиректит на HTTPS

Проверьте логи nginx:
```bash
docker-compose logs nginx | tail -50
```

Убедитесь, что в `nginx/conf.d/app.conf` есть редирект:
```nginx
location / {
    return 301 https://$host$request_uri;
}
```

## Готово! 🎉

Теперь ваше приложение работает через HTTPS. Все запросы на HTTP автоматически перенаправляются на HTTPS.

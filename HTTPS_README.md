# HTTPS Настройка - Готово! ✅

HTTPS настроен и готов к использованию с **самоподписанным сертификатом**.

## 🚀 Быстрый старт

### 1. Обновите `.env` на сервере

```bash
cd /opt/appointment-system
nano .env
```

Добавьте или убедитесь, что установлено:
```env
USE_HTTPS=True
ALLOWED_HOSTS=ваш_vps_ip
```

### 2. Запустите контейнеры

```bash
docker-compose down
docker-compose up -d
```

**Сертификат создастся автоматически!** При первом запуске nginx создаст самоподписанный сертификат.

### 3. Проверьте работу

```bash
# Проверьте логи nginx
docker-compose logs nginx | grep -i cert

# Должно показать: "Self-signed certificate created"
```

### 4. Откройте в браузере

```
https://YOUR_VPS_IP
```

⚠️ **Важно:** Браузер покажет предупреждение о самоподписанном сертификате - это нормально!

**Как принять сертификат:**
- Chrome/Edge: Нажмите "Дополнительно" → "Перейти на сайт"
- Firefox: Нажмите "Дополнительно" → "Принять риск и продолжить"

## 📋 Что уже настроено

✅ Nginx как reverse proxy  
✅ HTTPS конфигурация с самоподписанным сертификатом  
✅ Автоматическая генерация сертификата при первом запуске  
✅ HTTP → HTTPS редирект  
✅ Django settings для HTTPS  
✅ Скрипты для ручной генерации сертификата  

## 📁 Структура файлов

```
nginx/
├── nginx.conf           # Основная конфигурация nginx
├── conf.d/
│   └── app.conf        # Конфигурация для приложения (HTTP + HTTPS)
└── init-letsencrypt.sh # Скрипт для Let's Encrypt (если будет домен)

scripts/
├── generate-selfsigned-cert.sh  # Генерация самоподписанного сертификата
└── setup-https.sh               # Полная настройка HTTPS

certbot/
├── conf/               # SSL сертификаты (не коммитятся в git)
└── www/                # Для Let's Encrypt валидации
```

## 🔧 Ручная генерация сертификата

Если нужно пересоздать сертификат:

```bash
cd /opt/appointment-system

# Сделайте скрипт исполняемым
chmod +x scripts/generate-selfsigned-cert.sh

# Создайте сертификат
./scripts/generate-selfsigned-cert.sh

# Перезапустите nginx
docker-compose restart nginx
```

## 🔒 Переход на Let's Encrypt (когда появится домен)

Если у вас появится домен:

1. Обновите `nginx/conf.d/app.conf` - закомментируйте самоподписанный блок, раскомментируйте Let's Encrypt блок
2. Обновите `.env`: `ALLOWED_HOSTS=your-domain.com`
3. Запустите: `./nginx/init-letsencrypt.sh your-domain.com email@example.com`
4. Перезапустите: `docker-compose restart nginx web`

## ✅ Проверка работы

```bash
# Проверьте порты
ss -tlnp | grep -E ':(80|443)'

# Проверьте сертификат
docker-compose exec nginx ls -la /etc/letsencrypt/live/selfsigned/

# Тест HTTPS
curl -k https://YOUR_VPS_IP
```

## 🐛 Решение проблем

**Ошибка "SSL certificate not found":**
```bash
rm -rf certbot/conf/live/selfsigned
docker-compose restart nginx
```

**HTTP не редиректит на HTTPS:**
```bash
docker-compose logs nginx
# Проверьте, что в app.conf есть редирект
```

**Порт 443 занят:**
```bash
ss -tlnp | grep 443
# Остановите другой nginx или измените порт в docker-compose.yml
```

## 📚 Дополнительная документация

- `QUICK_START_HTTPS.md` - Быстрый старт
- `HTTPS_SETUP.md` - Подробная инструкция для Let's Encrypt

## ✨ Готово!

Ваше приложение теперь работает через HTTPS! 🎉

Все HTTP запросы автоматически перенаправляются на HTTPS.

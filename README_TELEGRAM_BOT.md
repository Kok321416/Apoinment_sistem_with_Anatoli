# 🤖 Telegram Бот - Отдельное приложение

Telegram бот вынесен в отдельное Django приложение `telegram_bot` для удобства управления и деплоя.

## 📁 Структура приложения

```
telegram_bot/
├── __init__.py
├── apps.py              # Конфигурация приложения
├── bot.py               # Основная логика бота
├── views.py             # Views для мини-приложения
├── urls.py              # URL маршруты
├── management/
│   └── commands/
│       └── run_bot.py   # Management команда для запуска
└── templates/
    └── telegram_bot/
        └── appointment.html  # Мини-приложение для записи
```

## 🚀 Быстрый старт

### Локальный запуск

```bash
python manage.py run_bot
```

### Деплой на Linux сервер

```bash
chmod +x deploy_bot.sh
./deploy_bot.sh
```

### Деплой на Windows сервер

```powershell
.\deploy_bot.ps1
```

## 🔧 Автоматический запуск при деплое

### Linux (systemd)

Скрипт `deploy_bot.sh` автоматически создает systemd service:

- **Файл**: `/etc/systemd/system/telegram-bot.service`
- **Автозапуск**: Включен при загрузке системы
- **Автоперезапуск**: При сбоях

**Управление:**
```bash
sudo systemctl status telegram-bot    # Статус
sudo systemctl start telegram-bot     # Запуск
sudo systemctl stop telegram-bot      # Остановка
sudo systemctl restart telegram-bot  # Перезапуск
sudo journalctl -u telegram-bot -f    # Логи
```

### Windows (Task Scheduler)

Скрипт `deploy_bot.ps1` создает задачу в планировщике:

- **Имя задачи**: `TelegramBotAppointmentSystem`
- **Автозапуск**: При входе пользователя
- **Автоперезапуск**: При сбоях (до 3 раз)

**Управление:**
```powershell
Get-ScheduledTaskInfo -TaskName TelegramBotAppointmentSystem  # Статус
Start-ScheduledTask -TaskName TelegramBotAppointmentSystem     # Запуск
Stop-ScheduledTask -TaskName TelegramBotAppointmentSystem      # Остановка
```

## 🔄 CI/CD интеграция

### GitLab CI/CD

Используйте `.gitlab-ci.yml` для автоматического деплоя при push в main.

### GitHub Actions

Используйте `.github/workflows/deploy-bot.yml` для автоматического деплоя.

**Настройка секретов:**
- `DEPLOY_HOST` - IP или домен сервера
- `DEPLOY_USER` - пользователь для SSH
- `DEPLOY_SSH_KEY` - приватный SSH ключ
- `DEPLOY_PATH` - путь к проекту на сервере

## 📱 Мини-приложение

Бот поддерживает Web App для записи на консультацию:

- **URL**: `/telegram/appointment/`
- **Открывается**: Через кнопку в боте
- **Требования**: HTTPS (для production)

## ⚙️ Настройки

В `settings.py`:

```python
# Токен бота
TELEGRAM_BOT_TOKEN = "ваш_токен"

# URL сайта (для мини-приложения)
SITE_URL = "https://yourdomain.com"  # Production
# SITE_URL = "http://127.0.0.1:8000"  # Development

# Использовать webhook (опционально)
TELEGRAM_USE_WEBHOOK = False
TELEGRAM_WEBHOOK_URL = "https://yourdomain.com/api/telegram/webhook/"
```

## 🐛 Отладка

### Проверка работы бота

1. Проверьте статус сервиса:
   ```bash
   sudo systemctl status telegram-bot
   ```

2. Просмотрите логи:
   ```bash
   sudo journalctl -u telegram-bot -f
   ```

3. Проверьте токен в настройках

4. Проверьте интернет-соединение

### Частые проблемы

**Бот не отвечает:**
- Проверьте, что сервис запущен
- Проверьте логи на ошибки
- Убедитесь, что токен правильный

**Мини-приложение не открывается:**
- Требуется HTTPS (для production)
- Проверьте `SITE_URL` в settings
- Убедитесь, что домен доступен

## 📚 Дополнительная документация

См. `TELEGRAM_BOT_SETUP.md` для подробной инструкции по настройке.

